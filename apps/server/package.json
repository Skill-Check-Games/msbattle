{
	"name": "server",
	"version": "1.0.0",
	"private": true,
	"description": "msbattle Node + socket.io server (lobby, matchmaking, puzzles, shop) and the static host for the built React client",
	"main": "src/minesweeperServer.js",
	"scripts": {
		"start": "node src/minesweeperServer.js",
		"dev": "DEV_AUTH=1 node src/minesweeperServer.js",
		"test": "node --test"
	},
	"dependencies": {
		"core": "*",
		"socket.io": "^4.7.5",
		"stripe": "^22.5.0"
	},
	"devDependencies": {
		"playwright": "^1.61.1",
		"socket.io-client": "^4.8.3"
	},
	"engines": {
		"node": ">=22"
	}
}
